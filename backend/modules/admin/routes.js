module.exports = function( app, AdminController, AdminModel ) {
    // Define routes here
     //app.all('/admin/:action/:id?', AdminController.attach());
     //app.all('/admin/?:action?', AdminController.attach());
}